#include<bits/stdc++.h>
using namespace std;

void removeDuplicates(vector<int> &v){
    int i=1;

    while (i<v.size()){
        if (v[i] == v[i-1]){
            v.erase(v.begin()+(i));
        }
        else{
            i++;
        }
    }

    return;
}


int main(){

    vector<int> v;

    int n;
    cin>>n;

    int num;
    
    for (int i=0; i<n; i++){
        cin>>num;
        v.push_back(num);
    }
    
    removeDuplicates(v);
    
    for (int i=0; i<v.size(); i++){
        cout<<v[i]<<" ";
    }
    cout<<endl;
    
    return 0;
}
